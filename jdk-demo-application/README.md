# HTTP Requests JDK Demo Application

This is an application that demos how to include the HTTP requests library in a standard Java
application. To run the project, simply use:

```
./gradlew run
```
